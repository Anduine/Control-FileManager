<Window x:Class="ControlFileManager.UI.Views.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ControlFileManager.UI.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:SearchViewModel}"
        Title="Пошук" 
        Height="360" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBgColor}"
        Foreground="{DynamicResource TextColor}">

  <Window.Resources>
    <Style TargetType="TextBlock" BasedOn="{StaticResource CTextBlock}">
      <Setter Property="Margin" Value="0,0,0,5"/>
      <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource CTextBox}">
      <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>
    <Style TargetType="CheckBox" BasedOn="{StaticResource CCheckBox}">
      <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>
  </Window.Resources>

  <Grid Margin="20">
    <StackPanel Margin="10">
      <TextBlock Text="Де розпочинати пошук:"/>
      <TextBox Text="{Binding SearchPath, Mode=TwoWay}"/>

      <TextBlock Text="Ім'я файлу (маска, наприклад *.txt):"/>
      <TextBox Text="{Binding NamePattern, Mode=TwoWay}"/>

      <TextBlock Text="Текст всередині файлу (опціонально):"/>
      <TextBox Text="{Binding ContentText, Mode=TwoWay}"/>

      <CheckBox IsChecked="{Binding IsRecursive, Mode=TwoWay}" Content="Шукати у вкладених каталогах"/>
      <CheckBox IsChecked="{Binding CaseSensitive, Mode=TwoWay}" Content="Враховувати регістр"/>

      <Grid Margin="0,15,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="10"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Content="Знайти" Command="{Binding ConfirmCommand}" IsDefault="True"/>

        <Button Grid.Column="2" Content="Відміна" IsCancel="True"/>
      </Grid>
    </StackPanel>
  </Grid>
</Window>
